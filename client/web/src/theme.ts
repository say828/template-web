export type AppTheme = {
  shellBg: string;
  cardRadius: string;
  cardPadding: string;
  border: string;
  accent: string;
  danger: string;
  muted: string;
  tableCellPy: string;
  tableCellPx: string;
  panelWidth: string;
};

export const appTheme: AppTheme = {
  shellBg: "#f5f7fb",
  cardRadius: "18px",
  cardPadding: "20px",
  border: "#e8edf5",
  accent: "#496fe8",
  danger: "#d95d4a",
  muted: "#708097",
  tableCellPy: "14px",
  tableCellPx: "16px",
  panelWidth: "340px",
};
